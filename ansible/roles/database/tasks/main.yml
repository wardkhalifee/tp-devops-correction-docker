- name: Run database container
  docker_container:
    name: mydb
    image: postgres:15
    env:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: mydb
    networks:
      - name: my_network
    state: started
    restart_policy: always